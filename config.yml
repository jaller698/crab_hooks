- name:        "hejsa"
  description: "Some long text"
  command:
    cmd: "cargo"
    args: "check"
    directory: "./"
  glob_pattern: ["**/*.rs", "*.toml", "*.yml"]

- name:        "foo"
  command:
    cmd: "cargo"
    args: "clippy --all-targets --all-features -- -Dwarnings"
  glob_pattern: ["**/*.rs"]

